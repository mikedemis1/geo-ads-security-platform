[pytest]
testpaths = tests
filterwarnings =
    error
    # starlette's TestClient still uses a deprecated anyio alias; not ours to fix
    ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning
