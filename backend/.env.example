# REQUIRED — app will crash if these are missing
JWT_SECRET=CHANGE_ME_32BYTES_DEV_ONLY_00000
ADMIN_TOKEN_SECRET=CHANGE_ME_32BYTES_DEV_ONLY_000000
ADMIN_USER=admin
ADMIN_PASS=geo2025

# Optional (with defaults)
JWT_ISSUER=geo-ads
JWT_AUDIENCE=geo-ads-ui
JWT_DEFAULT_TTL_SECONDS=3600
REFRESH_TOKEN_TTL_SECONDS=86400
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:8080
